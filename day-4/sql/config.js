// 因为经常要用，单独来出来，放到一个对象中，这样下次就不需要在重新写一遍了，
// 如果要用，直接导入这个JS文件就可以了，
module.exports = {
    // 域名
    host: 'localhost',
    // 用户名
    user: 'root',
    // 密码
    password: '',
    // 数据库名字
    database : 'lj'
}